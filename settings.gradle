pluginManagement {
    repositories {
        maven {
            url "$artifactory_contextUrl/gradle-plugins-virtual"
            credentials {
                username = "${artifactory_user}"
                password = "${artifactory_password}"
            }
        }
    }
}
include 'fvt'
